<div class="container">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data- target="#navbar1" aria-expanded="false">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#">SachOnline</a>
    </div>  
    @{ var customer = Session["TaiKhoan"] as SachOnline.Models.KHACHHANG;}
    <div class="collapse navbar-collapse" id="navbar1">
        <ul class="nav navbar-nav">
            <li class="active"><a href="@Url.Action("Index", "SachOnline")">TRANG CHỦ</a> </li>
            <li class="active"><a href="#">GIỚI THIỆU</a> </li>
            <li class="active"><a href="#">LIÊN HỆ</a> </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
                @if (customer == null)
                {
                    <ul class="nav navbar-nav">
                        <li>
                            <a href="~/Account/DangKy">
                                <span class="glyphicon glyphicon-user"></span>
                                Đăng ký
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("DangNhap", "Account", new { url = Request.Url.AbsoluteUri })">
                                <span class="glyphicon glyphicon-log-in"></span> Đăng nhập
                            </a>
                        </li>                   
                    </ul>
                }
            else
            {
            <ul class="nav navbar-nav">
                            <li>
                                <a href="#">
                                    <span class="glyphicon glyphicon-user"></span> Xin chào:@customer.HoTen
                                </a>
                            </li>

                            <li>
                                <a href="@Url.Action("Logout","Account")">
                                    <span class="glyphicon glyphicon-log-out"></span> Đăng xuất
                                </a>
                            </li>

            </ul>
            }
                 

            </li>
            <li style=" margin-top: -16px;">
                @Html.Action("GioHangPartial","GioHang")"
            </li>
        </ul>
        <form class="navbar-form navbar-right" role="search">
            <div class="form-group">
                <input type="text" class="form-control" placeholder="Tìm kiếm">
            </div>
            <button type="submit" class="btn btn-default">Tìm</button>
        </form>
    </div>
    <!--Tìm kiếm-->
</div>
<!--End-Phần tìm kiếm-->